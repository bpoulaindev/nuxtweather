export interface ComputedClasses {
  background: string;
  text: string;
  iconBg: string;
  button: string;
  ring: string;
  ping: {
    bg: string;
    ping: string;
  };
  cards: {
    bg: string;
    text: string;
  };
  settings: {
    trigger: string;
    icon: string;
    arrow: string;
  };
}
