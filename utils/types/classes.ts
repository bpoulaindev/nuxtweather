export interface ComputedClasses {
  background: string;
  text: string;
  iconBg: string;
  button: string;
  ping: {
    bg: string;
    ping: string;
  };
  cards: {
    bg: string;
    text: string;
  };
  settings: {
    trigger: string;
    icon: string;
    arrow: string;
  };
}
